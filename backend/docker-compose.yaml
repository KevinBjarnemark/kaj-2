services:

  db:
     image: mysql:8.4.0
     ports:
       - ${DB_PORT}:3306
     environment:
       - MYSQL_ROOT_PASSWORD=${DB_PASSWORD}
       - MYSQL_DATABASE=${DB_NAME}
     volumes:
       - db_data:/var/lib/mysql
volumes:
  db_data:
